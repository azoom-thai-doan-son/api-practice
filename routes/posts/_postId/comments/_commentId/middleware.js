/**
 * @type {import('express').RequestHandler}
 */
const getComment = async (req, res) => {}

/**
 * @type {import('express').RequestHandler}
 */
const verifyPermissionOnComment = async (req, res) => {}

export { getComment as middleware1, verifyPermissionOnComment as middleware2 }
